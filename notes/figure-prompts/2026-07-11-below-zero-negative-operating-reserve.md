# Figure prompts — 2026-07-11-below-zero-negative-operating-reserve

All three figures are matplotlib data figures generated by
`calcs/below-zero/figures.py` (reads `neg_c3.csv.gz` / `all_c3.csv.gz` written by
`calcs/below-zero/compute.py`). They already live in `/images/` and follow §5's
data-figure rule: lettered callouts only, every letter defined in the caption.
Palette: outline `#143f33`; teal fills `#2f7da3`, lifted `#5b9fc0`, deep
`#1c5572`; cream `#f7f1d7` on page `#f7f4ee`.

## Figure 1 — hero / OG card (1200×630 landscape)

File: `/images/2026-07-11-below-zero-hero.png` (data figure, ready to ship).
A single 100%-wide horizontal bar of the below-zero population, split into the
three mutually exclusive causes — **C** asset-rich/reserve-poor (58%),
**A** fully underwater (32%), **B** unrestricted deficit only (10%) — with
bracket **D** marking the true-distress subset beneath A. Front matter
`og-image` points here.

**Optional upgrade — house-illustration hero.** If Peter wants the hand-drawn
style to match the parent post's fuel-gauge hero, regenerate with Illustrator
Text-to-Vector (open the gear logo as the color source first):

> Flat vector illustration, bold uniform dark-green outlines, limited palette of
> teal and cream. A single large fuel gauge, needle dropped below the empty peg
> into a thin danger zone. Behind it, a cutaway shows the tank is not empty —
> it is packed with a small building/roof icon and a padlocked box (restricted
> funds), so the reserve reads empty while assets sit locked inside. Horizontal
> left-to-right composition, landscape 1200×630.

Suggested UPPERCASE caption layer (generator drops free text): "RESERVE: EMPTY"
by the gauge, "ASSETS: LOCKED" by the cutaway. If swapped in, keep the filename
so `og-image` still resolves.

## Figure 3 — below-zero share by expense band

File: `/images/2026-07-11-below-zero-by-size.png`. Bars = share below zero per
band (rising 13→28%); overlaid line = share fully underwater (flat ~6–8%).
**A** = the >$50M bar; **B** = the underwater line at that band.

## Figure 2 — liquidity of the below-zero group

File: `/images/2026-07-11-below-zero-liquidity.png`. Histogram of literal
months of cash on hand among below-zero charities. **A** = the zero line,
**B** = the median (1.3 months), **C** = the three-month mark, **D** = the
detached overflow bin for charities holding more than a year of cash.
